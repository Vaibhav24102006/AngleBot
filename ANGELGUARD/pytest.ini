[pytest]
# Single, canonical test command for the project: `pytest` from ANGELGUARD/.
# Only tests/ is discovered — analysis/manual_*_check.py are CLI diagnostic
# scripts, not automated tests, and live outside this tree on purpose.
testpaths = tests
python_files = test_*.py
